{{- if .Values.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "video-processor.fullname" . }}
  labels:
    {{- include "video-processor.labels" . | nindent 4 }}
    release: kps
spec:
  selector:
    matchLabels:
      {{- include "video-processor.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
{{- end }}
