apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-inference-stub.fullname" . }}
  labels:
    {{- include "ai-inference-stub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-inference-stub.selectorLabels" . | nindent 4 }}
