{{- if .Values.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "ai-inference-stub.fullname" . }}
  labels:
    {{- include "ai-inference-stub.labels" . | nindent 4 }}
    release: kps
spec:
  selector:
    matchLabels:
      {{- include "ai-inference-stub.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
{{- end }}
