apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-inference-stub.fullname" . }}-test-connection"
  labels:
    {{- include "ai-inference-stub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-inference-stub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
